import React, { memo } from 'react';
import { MenuUnfoldOutlined } from '@ant-design/icons';

const NoticeIconView = memo(() => {
  return (
    <>
      <MenuUnfoldOutlined />
    </>
  );
});

export default NoticeIconView;
